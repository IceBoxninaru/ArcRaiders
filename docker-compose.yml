services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: arcraiders-web:latest
    container_name: arcraiders-web
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - '80:8000'
